apiVersion : apps/v1
kind : Service
metadata :
  name : nginx-service
  labels :
    app : nginx-webapp

  spec :
    selector :
      app : nginx-webapp
    ports :
      - protocol : TCP
        port : 80
        targetPort : 80
    type : NodePort